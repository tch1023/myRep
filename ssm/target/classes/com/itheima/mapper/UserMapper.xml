<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.UserMapper">

    <select id="login" resultType="user" parameterType="user">
        SELECT  * FROM user WHERE username=#{username} AND pwd=#{pwd}
    </select>

    <!--查询所有用户信息-->
    <select id="findAll" resultType="user" parameterType="pageBean">
         SELECT  * FROM user limit #{begin},#{pageSize}
    </select>
    <!--保存用户-->
    <insert id="saveUser" parameterType="user">
        insert into user(username,pwd,pic,sex,age,address,qq,email) values(#{username},#{pwd},#{pic},#{sex},#{age},#{address},#{qq},#{email})
    </insert>
    <!--删除用户-->
    <delete id="deleteUser" parameterType="int">
        delete from user where id = #{id}
    </delete>
    <!--查询总记录数-->
    <select id="findCount" resultType="int">
        select count(*) from user
    </select>
    <!--查询一个用户-->
    <select id="findOne" parameterType="int" resultType="user">
        select * from user where id = #{id}
    </select>
    <!--修改用户-->
    <update id="updateUser" parameterType="user">
        update user set username = #{username},pwd = #{pwd},sex = #{sex},age=#{age},address=#{address},qq=#{qq},email=#{email} where id = #{id}
    </update>
</mapper>